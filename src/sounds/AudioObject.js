//import {Audio} from "expo-av";

export let AudioObject = (function () {
    let instance;

    function createInstance() {
        //let object = new Audio.Sound();
        //return object;
    }

    return {
        getInstance: function () {
            //if (!instance) {
              //  instance = createInstance();
            //}
            //return instance;
        }
    };
})();
